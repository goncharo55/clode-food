# Claude Code利用とは別に、Anthropic Consoleで発行する単体のAPIキー
# https://console.anthropic.com
ANTHROPIC_API_KEY=

# apps/web の /api/ingest エンドポイント（本番ではデプロイ先のURLに変更）
INGEST_API_URL=http://localhost:3000/api/ingest

# apps/web/.env のINGEST_SECRETと同じ値を設定する
INGEST_SECRET=dev-ingest-secret-change-me
